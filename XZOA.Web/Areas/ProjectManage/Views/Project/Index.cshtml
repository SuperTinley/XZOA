
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Index.cshtml";
}

<script>
    $(function () {
        gridList();
    })
    function gridList() {
        var $gridList = $("#gridList");
        $gridList.dataGrid({
            url: "/ProjectManage/Project/GetList",
            height: $(window).height() - 128,
            colModel: [
                {   label: "主键", name: "F_Id", hidden: true, key: true },
                {
                    label: "操作", name: "", width: 90, align: "left",
                    formatter: function (cellvalue, options, rowObject) {
                        var func = "btn_details('" + rowObject.F_Id + "')";
                        var func1 = "btn_finish('" + rowObject.F_Id + "',"+ rowObject.F_F_Choose +")";
                        return "<a  onclick=" + func + ">查看</a>&nbsp;&nbsp;<a  onclick=" + func1 + ">完成</a>";
                    }
                },
                {   label: '项目工程师', name: 'F_ProjectEngineer', width: 65, align: 'left' },
                {   label: '客户', name: 'F_Customer', width: 40, align: 'left' },
                {   label: '型号', name: 'F_Model', width: 60, align: 'left' },
                {   label: '名称规格', name: 'F_Spc', width: 60, align: 'left' },
                {   label: '样板数量', name: 'F_SampleQty', width: 50, align: 'left'},
                {
                    label: "类型", name: "F_TypeID", width: 45, align: "left",
                    formatter: function (cellvalue, options, rowObject) {
                       return top.clients.type[cellvalue] == null ? "" : top.clients.type[cellvalue].fullname;
                    }
                },
                {
                    label: '订单号', name: 'F_OrderNo', width: 60, align: 'left'
                },
                {
                    label: '立项单', name: 'F_OrderItem', width: 60, align: 'left'
                },
                {
                    label: "立项时间", name: "F_OrderItemDate", width: 75, align: 'left',
                    formatter: "date", formatoptions: { srcformat: 'Y-m-d', newformat: 'Y-m-d' }
                },
                {
                    label: '计划寄样时间', name: 'F_PlanSendSampleDate', width: 75, align: 'left',
                    formatter: "date", formatoptions: { srcformat: 'Y-m-d', newformat: 'Y-m-d' }
                },
                {
                    label: "样板回厂时间", name: "F_SampleReturnDate", width: 75, align: "left",
                    formatter: "date", formatoptions: { srcformat: 'Y-m-d', newformat: 'Y-m-d' }
                },
                {
                    label: "备注", name: "F_Remark", width: 70, align: "left",
                },
                {
                    label: "自制/外协", name: "F_Choose", width: 38, align: "left",
                    formatter: function (cellvalue, options, rowObject) {
                       return top.clients.dataItems["F_Choose"] == null ? "" : top.clients.dataItems["F_Choose"][cellvalue];
                    }
                }
            ],
            ondblClickRow: function (rowId) {
                $.modalOpen({
                    id: "Form",
                    title: "新建",
                    url: "/ProjectManage/Project/ProjectForm?keyValue=" + keyValue,
                    width: "660px",
                    height: "600px",
                    callBack: function (iframeId) {
                        top.frames[iframeId].submitForm();
                    }
                });
            },
            pager: "#gridPager",
            sortname: 'date desc',
            multiselect: true,
            rowNum: 20,
            rowList: [10, 20, 30],
            viewrecords: true
        });
        $("#btn_search").click(function () {
            $gridList.jqGrid('setGridParam', {
                postData: {
                    keyword: $("#txt_keyword").val()
                },
                page: 1 }).trigger('reloadGrid');
        });
    }

    function btn_finish(keyValue,type) {
        $.modalOpen({
            id: "Form",
            title: "完成样板",
            url: "/ProjectManage/Project/buyForm?keyValue=" + keyValue + "&&type=" + type,
            width: "660px",
            height: "300px",
            callBack: function (iframeId) {
                top.frames[iframeId].submitForm();
            }
        });
    }

    function btn_details(keyValue) {
        $.modalOpen({
            id: "Details",
            title: "详情",
            url: "/ProjectManage/Project/Details?keyValue=" + keyValue,
            width: "660px",
            height: "700px",
            callBack: function (iframeId) {
                top.frames[iframeId].submitForm();
            }
        });
    }

    function btn_add() {
        $.modalOpen({
            id: "Form",
            title: "新建",
            url: "/ProjectManage/Project/ProjectForm",
            width: "660px",
            height: "600px",
            callBack: function (iframeId) {
                top.frames[iframeId].submitForm();
            }
        });
    }
</script>

<div class="topPanel">
    <div class="toolbar">
        <div class="btn-group">
            <a class="btn btn-primary" onclick="$.reload()"><span class="glyphicon glyphicon-refresh"></span></a>
        </div>
        <div class="btn-group">
            <a id="project_add" authorize="yes" class="btn btn-primary dropdown-text" onclick="btn_add()"><i class="fa fa-plus"></i>新建</a>
        </div>
        <div class="btn-group">
            <a id="project_delete" authorize="yes" class="btn btn-primary dropdown-text" onclick="btn_delete()">删除</a>
        </div>
        <script>$('.toolbar').authorizeButton()</script>
    </div>
    <div class="search">
        <table>
            <tr>
                <td>
                    <form class="form-inline" role="form">
                        @*<div class="form-group">
                            <div class="input-group">
                                <input id="txt_keyword" type="text" class="form-control" placeholder="申购人/名称/采购员" style="width: 170px;">
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="daterange-date">申购日期:</label>
                            <div class="input-group">
                                <input id="dateBeginDate" type="hidden" />
                                <input id="dateEndDate" type="hidden" />
                                <input type="text" id="date" placeholder="请选择日期" class="form-control" style="width:170px;" />
                            </div>
                        </div>*@
                        <div class="form-group">
                            <button id="btn_search" type="button" class="btn  btn-primary"><i class="fa fa-search"></i></button>
                        </div>
                    </form>
                </td>
            </tr>
        </table>
    </div>
</div>
<div class="gridPanel">
    <table id="gridList"></table>
    <div id="gridPager"></div>
</div>

